# 새 채팅에 넘기는 인계문 (2026-09-08)

이 파일을 새 채팅에 통째로 붙여넣거나, `/Users/leeajun/Projects/vivivic-docs/HANDOFF.md`
를 읽으라고 하면 됩니다. `CLAUDE.md` 도 같이 읽어야 합니다.

---

## 프로그램

**VIVIVIC 서류관리** — 구글드라이브 `중국` 폴더의 파일명과 **파일 내용**을 읽어
거래처 · 차수 · 7단계를 판정하고, Firebase에 올려 팀이 같이 봅니다.
파일 본체는 옮기지 않고 목록만 다룹니다.

- 라이브: https://vivivic-cloud.github.io/vivivic-docs/ (v0.19.0)
- 저장소: https://github.com/vivivic-cloud/vivivic-docs
- 로컬: `/Users/leeajun/Projects/vivivic-docs` (**git 저장소 아님** — 아래 참고)

---

## 끝난 일 — 소스와 배포본을 맞췄습니다 (2026-09-08)

번들에만 있던 고침 아홉 가지를 소스로 옮겼습니다. **이제 `python3 build-single.py` 결과가
살아 있는 배포본과 같습니다.** 빌드해서 올려도 안전합니다. `v0.19.1` 로 나가 있습니다.

옮기면서 배포본에 있던 버그 둘이 드러나 함께 고쳤습니다.

- `saveRules` 의 `batch.commit()` 이 반복문 밖에 있어 `ReferenceError` 로 터졌습니다.
  **사용자 규칙 저장이 라이브에서 아예 안 되고 있었습니다.**
- 숫자판이 `md:grid-cols-3` 을 쓰는데 CSS 를 다시 빌드하지 않아 그 클래스가 없었습니다.

### 남은 것 하나 — 정하고 고쳐야 합니다

`node --test test/*.test.mjs` 가 41건 통과, 1건 실패합니다.
`buildBatches — 잔액서류가 이미 있으면 추정으로 얹지 않는다`.

`F-023形式发票和装箱单.xlsx` 처럼 PI 와 패킹리스트가 한 이름에 다 든 파일이 문제입니다.
PI 우선 규칙(`a165840`)을 넣으면 이 파일이 PI 가 되고, 차수가 안 적혀 있으니
**날짜가 가까운 차수에 추정으로 붙습니다.** 예전에는 잔액서류로 분류돼
"이미 있으면 얹지 않는다" 규칙에 걸려 미분류로 남았습니다.

지금 라이브가 이미 이렇게 돌고 있어 새로 생긴 문제는 아닙니다. 둘 중 하나로 정해야 합니다.

- **(A)** 차수 없는 PI 는 붙이지 말고 미분류로 — "없는 자료를 추정하지 않는다"는 원칙대로
- **(B)** 지금처럼 날짜가 확실히 가까우면 붙이고 "추정"이라고 표시

### 그 다음 — 번들 없애기

소스와 배포본 두 벌이 있는 건 설계가 아니라 사고입니다. 번들이 남아 있는 한
급할 때 배포본을 직접 고치는 유혹이 있고, 그러면 같은 사고가 재발합니다.
이 앱은 빌드 없이 브라우저에서 바로 도는 ESM이라 번들이 원래 필요 없습니다.
`docs/` 에 `index.html`·`js/`·`css/` 를 그대로 두고 Pages 가 그걸 내보내게 하면
파일이 한 벌만 남습니다. 데모용 단일 파일은 사용자가 필요 없다고 했습니다.

## 배포

Pages는 **`main` 브랜치의 `/docs`** 를 봅니다 (오늘 루트에서 옮겼습니다).
`build-single.py` 가 `docs/index.html` 을 직접 만들므로, 커밋해서 밀면 그게 배포입니다.

판올림하면 `index.html` 안 **버전 문자열 두 곳**을 같이 고쳐야 합니다
(로그인 카드 + 헤더의 `tracking-wider` 스팬). `checkForUpdate()` 가 이걸 읽습니다.

**사용자 요청: 배포는 묻지 말고 자동으로.** 고치면 빌드·푸시·확인까지 이어서 합니다.

---

## Apps Script (서버 동기화)

`appsscript/sync.gs` 를 `clasp push -f` 로 밀고,
`clasp redeploy AKfycbx0Vxa64caw5r-wDLwK-KCfaBO0AciL3SOS2u7JXGmZ30mtMeQDjkkmQHU49rWmdxng -d "설명"`
으로 **고정 배포에 덮어씁니다**. 새 배포를 만들면 주소가 바뀝니다.

**동기화는 타이머가 아니라 앱이 부릅니다.** 앱을 켜거나 새로고침하면 한 번 부릅니다.
드라이브를 훑어 지문을 떠 보고 지난번과 같으면 Firestore를 안 건드립니다 (비용 때문).

**서류에서 뽑는 규칙을 고치면 `READ_VERSION` 을 올립니다.** `MARK_VERSION` 만 올리면
"이미 읽었다"고 넘어가 정작 다시 안 읽습니다. 다 읽는 데 sync를 서너 번 불러야 합니다
(한 번에 4분까지만 읽고 나머지는 다음 호출로 미룹니다).

---

## 최근에 넣은 기능 (v0.17 ~ v0.19)

같은 칸에 서류가 여러 장이면, 각 장이 **앞 판과 무엇이 다른지**를 표로 보여줍니다.

| 구분 | 항목 | 이전 | 이후 | 증감 |
|---|---|---:|---:|---:|
| 수량 | 비요크 무헤드 침대 Q/K `ZBY_QKN` | 30 | 56 | +26 |
| 합계 | 수량 | 215 | 266 | +51 |

- 엑셀은 `readItems(rows)`, 발주서 PDF는 `readPdfItems(text)` 가 품목을 뽑습니다
- PDF는 구글 문서로 바꾸면 표가 "탭 + 칸"으로 풀립니다. 칸 차례가 판마다 달라서,
  자리를 세지 않고 **금액 → 수량 → 금액** 이 이어지는 자리를 찾고 앞 두 칸을 코드·색으로 봅니다
- 검산: 뽑은 수량 합이 PDF의 TOTAL과 일치 (26-17 215·266, 26-19 962)
- 내용이 같은 파일은 md5로 묶어 하나만 보여줍니다

---

## 열려 있는 숙제

- `F-023形式发票和装箱单发给客户.xlsx`, `F-026`·`F-027` PDF는 스캔본이라 자동 분류 불가.
  수동 지정할지 사용자에게 물어봐야 합니다.
- 이 로컬 폴더는 **git 저장소가 아닙니다.** 소스는 GitHub API로 밀어 넣었습니다.
  터미널에서 한 번만 해두면 편해집니다:

  ```sh
  cd /Users/leeajun/Projects/vivivic-docs
  git init -b main
  git remote add origin https://github.com/vivivic-cloud/vivivic-docs.git
  git fetch origin main && git reset --mixed FETCH_HEAD && git status --short
  ```

- `.github/workflows/pages.yml` 은 저장소에 없습니다 (토큰에 workflow 권한 없음).
  한 번도 돈 적 없는 죽은 파일이고, Pages가 `docs/` 를 직접 보므로 필요 없습니다.

---

## 사용자가 반복해서 말한 것

- **폴더를 믿지 마세요.** 사람은 늘 실수합니다. **실제 파일명과 내용**으로 판정합니다.
- **차수는 내부에서 만든 허상입니다.** 중요한 건 하나의 발주에서 시작된 서류를 묶는 것.
- **없는 자료를 달라고 하지 마세요.** 있는 현실에서 풀어야 합니다.
- 중국어 파일명은 발주서가 될 수 없습니다. `合同` 류는 계약금서류입니다.
- 표기는 시안성이 중요합니다. 값을 늘어놓지 말고 **달라진 것만** 보여줍니다.
- 배포는 묻지 말고 자동으로.
